def test():
    res = ""
    for i in ["H", "E", "L", "L", "O"]:
        res = res + i
    return res

def test2():
    if file.exists("/etc/passwd"):
       return str(file.read("/etc/passwd"))

file.append("/tmp/win", test())
str(test2())