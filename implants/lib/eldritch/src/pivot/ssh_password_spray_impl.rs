use anyhow::Result;

pub fn ssh_password_spray(
    _targets: Vec<String>,
    _port: i32,
    _credentials: Vec<String>,
    _keys: Vec<String>,
    _command: String,
    _shell_path: String,
) -> Result<String> {
    unimplemented!("Method unimplemented")
}
