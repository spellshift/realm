[package]
name = "eldritch-libregex"
version = "0.3.2"
edition = "2024"

[dependencies]
eldritch-core = { workspace = true }
eldritch-macros = { workspace = true }
regex = { workspace = true, optional = true }

[features]
default = ["stdlib"]
stdlib = ["dep:regex"]
fake_bindings = []
