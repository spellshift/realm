[package]
name = "eldritch-mockagent"
version = "0.1.0"
edition = "2024"

[dependencies]
eldritch-agent = { path = "../../eldritch-agent" }
pb = { path = "../../../pb" }
tokio = { workspace = true, features = ["sync"] }
async-trait = { workspace = true }

[features]
verbose-logging = ["eldritch-agent/verbose-logging", "pb/verbose-logging"]
