[package]
name = "eldritch-macros"
version = "0.3.0"
edition = "2024"

[features]

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0", features = ["full", "extra-traits"] }
quote = "1.0"
proc-macro2 = "1.0"

[dev-dependencies]
eldritch-core = { workspace = true }
