[package]
name = "eldritch-repl"
version = "0.3.0"
edition = "2024"

[features]

[[bench]]
name = "repl_bench"
harness = false

[dependencies]
eldritch-core = { workspace = true }
spin = { version = "0.10.0", features = ["rwlock"] }
log = "0.4"

[dev-dependencies]
criterion = "0.5"
