[package]
name = "eldritch-libevents"
version = "0.1.0"
edition = "2024"

[dependencies]
eldritch-core = { workspace = true }
eldritch-macros = { workspace = true }
eldritch-agent = { workspace = true }
anyhow = { version = "1.0" }
rust-embed = { version = "8.0" }
spin = { workspace = true }

[features]
default = ["std", "stdlib"]
std = ["eldritch-core/std"]
fake_bindings = []
stdlib = []
