[package]
name = "portal-stream"
version = "0.3.0"
edition = "2024"

[features]
default = ["tokio"]
tokio = ["dep:tokio"]

[dependencies]
pb = { workspace = true }
anyhow = { workspace = true }
tokio = { workspace = true, features = ["sync"], optional = true }
