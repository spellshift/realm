[package]
name = "screenshot"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = { workspace = true }

[target.'cfg(windows)'.dependencies.windows-sys]
workspace = true
features = [
    "Win32_Graphics_Gdi",
    "Win32_UI_WindowsAndMessaging",
    "Win32_Foundation",
]
