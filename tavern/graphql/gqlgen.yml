schema:
  - schema/scalars.graphql
  - schema/schema.graphql
  - schema/user.graphql

model:
  package: graphql

exec:
  layout: follow-schema
  dir: ./generated
  package: generated

resolver:
  # Tell gqlgen to generate resolvers next to the schema file.
  layout: follow-schema
  dir: .
  package: graphql

# gqlgen will search for any type names in the schema in the generated
# ent package. If they match it will use them, otherwise it will new ones.
autobind:
  - github.com/kcarretto/realm/tavern/ent
  - github.com/kcarretto/realm/tavern/ent/file
  - github.com/kcarretto/realm/tavern/ent/user
  - github.com/kcarretto/realm/tavern/ent/tag
  - github.com/kcarretto/realm/tavern/ent/target
  - github.com/kcarretto/realm/tavern/ent/job
  - github.com/kcarretto/realm/tavern/ent/task
  - github.com/kcarretto/realm/tavern/ent/tome
 
struct_tag: json

models:
  ID:
    model:
      - github.com/99designs/gqlgen/graphql.IntID
  Node:
    model:
      # ent.Noder is the new interface generated by the Node template.
      - github.com/kcarretto/realm/tavern/ent.Noder
      # - entgo.io/contrib/entgql/internal/todo/ent.Noder
 
  # Custom mapping from GraphQL `TagKind`
  # to the generated `tag.Kind` enum type.
  TagKind:
    model:
      - github.com/kcarretto/realm/tavern/ent/tag.Kind 

  # Custom mapping from GraphQL `CredentialKind`
  # to the generated `credential.Kind` enum type.
  # CredentialKind:
  #   model:
  #     - github.com/kcarretto/realm/tavern/ent/credential.Kind 