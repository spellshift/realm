// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"realm.pub/tavern/internal/ent/adventure"
)

// Adventure is the model entity for the Adventure schema.
type Adventure struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Timestamp of when this ent was created
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Timestamp of when this ent was last updated
	LastModifiedAt time.Time `json:"last_modified_at,omitempty"`
	// Name of the adventure
	Name string `json:"name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AdventureQuery when eager-loading is set.
	Edges        AdventureEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AdventureEdges holds the relations/edges for other nodes in the graph.
type AdventureEdges struct {
	// Quests that belong to this adventure
	Quests []*Quest `json:"quests,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int

	namedQuests map[string][]*Quest
}

// QuestsOrErr returns the Quests value or an error if the edge
// was not loaded in eager-loading.
func (e AdventureEdges) QuestsOrErr() ([]*Quest, error) {
	if e.loadedTypes[0] {
		return e.Quests, nil
	}
	return nil, &NotLoadedError{edge: "quests"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Adventure) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case adventure.FieldID:
			values[i] = new(sql.NullInt64)
		case adventure.FieldName:
			values[i] = new(sql.NullString)
		case adventure.FieldCreatedAt, adventure.FieldLastModifiedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Adventure fields.
func (a *Adventure) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case adventure.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			a.ID = int(value.Int64)
		case adventure.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				a.CreatedAt = value.Time
			}
		case adventure.FieldLastModifiedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_modified_at", values[i])
			} else if value.Valid {
				a.LastModifiedAt = value.Time
			}
		case adventure.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				a.Name = value.String
			}
		default:
			a.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Adventure.
// This includes values selected through modifiers, order, etc.
func (a *Adventure) Value(name string) (ent.Value, error) {
	return a.selectValues.Get(name)
}

// QueryQuests queries the "quests" edge of the Adventure entity.
func (a *Adventure) QueryQuests() *QuestQuery {
	return NewAdventureClient(a.config).QueryQuests(a)
}

// Update returns a builder for updating this Adventure.
// Note that you need to call Adventure.Unwrap() before calling this method if this Adventure
// was returned from a transaction, and the transaction was committed or rolled back.
func (a *Adventure) Update() *AdventureUpdateOne {
	return NewAdventureClient(a.config).UpdateOne(a)
}

// Unwrap unwraps the Adventure entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (a *Adventure) Unwrap() *Adventure {
	_tx, ok := a.config.driver.(*txDriver)
	if !ok {
		panic("ent: Adventure is not a transactional entity")
	}
	a.config.driver = _tx.drv
	return a
}

// String implements the fmt.Stringer.
func (a *Adventure) String() string {
	var builder strings.Builder
	builder.WriteString("Adventure(")
	builder.WriteString(fmt.Sprintf("id=%v, ", a.ID))
	builder.WriteString("created_at=")
	builder.WriteString(a.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_modified_at=")
	builder.WriteString(a.LastModifiedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(a.Name)
	builder.WriteByte(')')
	return builder.String()
}

// NamedQuests returns the Quests named value or an error if the edge was not
// loaded in eager-loading with this name.
func (a *Adventure) NamedQuests(name string) ([]*Quest, error) {
	if a.Edges.namedQuests == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := a.Edges.namedQuests[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (a *Adventure) appendNamedQuests(name string, edges ...*Quest) {
	if a.Edges.namedQuests == nil {
		a.Edges.namedQuests = make(map[string][]*Quest)
	}
	if len(edges) == 0 {
		a.Edges.namedQuests[name] = []*Quest{}
	} else {
		a.Edges.namedQuests[name] = append(a.Edges.namedQuests[name], edges...)
	}
}

// Adventures is a parsable slice of Adventure.
type Adventures []*Adventure
