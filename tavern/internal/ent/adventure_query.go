// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"realm.pub/tavern/internal/ent/adventure"
	"realm.pub/tavern/internal/ent/predicate"
	"realm.pub/tavern/internal/ent/quest"
)

// AdventureQuery is the builder for querying Adventure entities.
type AdventureQuery struct {
	config
	ctx             *QueryContext
	order           []adventure.OrderOption
	inters          []Interceptor
	predicates      []predicate.Adventure
	withQuests      *QuestQuery
	modifiers       []func(*sql.Selector)
	loadTotal       []func(context.Context, []*Adventure) error
	withNamedQuests map[string]*QuestQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AdventureQuery builder.
func (aq *AdventureQuery) Where(ps ...predicate.Adventure) *AdventureQuery {
	aq.predicates = append(aq.predicates, ps...)
	return aq
}

// Limit the number of records to be returned by this query.
func (aq *AdventureQuery) Limit(limit int) *AdventureQuery {
	aq.ctx.Limit = &limit
	return aq
}

// Offset to start from.
func (aq *AdventureQuery) Offset(offset int) *AdventureQuery {
	aq.ctx.Offset = &offset
	return aq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aq *AdventureQuery) Unique(unique bool) *AdventureQuery {
	aq.ctx.Unique = &unique
	return aq
}

// Order specifies how the records should be ordered.
func (aq *AdventureQuery) Order(o ...adventure.OrderOption) *AdventureQuery {
	aq.order = append(aq.order, o...)
	return aq
}

// QueryQuests chains the current query on the "quests" edge.
func (aq *AdventureQuery) QueryQuests() *QuestQuery {
	query := (&QuestClient{config: aq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(adventure.Table, adventure.FieldID, selector),
			sqlgraph.To(quest.Table, quest.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, adventure.QuestsTable, adventure.QuestsColumn),
		)
		fromU = sqlgraph.SetNeighbors(aq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Adventure entity from the query.
// Returns a *NotFoundError when no Adventure was found.
func (aq *AdventureQuery) First(ctx context.Context) (*Adventure, error) {
	nodes, err := aq.Limit(1).All(setContextOp(ctx, aq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{adventure.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aq *AdventureQuery) FirstX(ctx context.Context) *Adventure {
	node, err := aq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Adventure ID from the query.
// Returns a *NotFoundError when no Adventure ID was found.
func (aq *AdventureQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aq.Limit(1).IDs(setContextOp(ctx, aq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{adventure.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aq *AdventureQuery) FirstIDX(ctx context.Context) int {
	id, err := aq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Adventure entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Adventure entity is found.
// Returns a *NotFoundError when no Adventure entities are found.
func (aq *AdventureQuery) Only(ctx context.Context) (*Adventure, error) {
	nodes, err := aq.Limit(2).All(setContextOp(ctx, aq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{adventure.Label}
	default:
		return nil, &NotSingularError{adventure.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aq *AdventureQuery) OnlyX(ctx context.Context) *Adventure {
	node, err := aq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Adventure ID in the query.
// Returns a *NotSingularError when more than one Adventure ID is found.
// Returns a *NotFoundError when no entities are found.
func (aq *AdventureQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aq.Limit(2).IDs(setContextOp(ctx, aq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{adventure.Label}
	default:
		err = &NotSingularError{adventure.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aq *AdventureQuery) OnlyIDX(ctx context.Context) int {
	id, err := aq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Adventures.
func (aq *AdventureQuery) All(ctx context.Context) ([]*Adventure, error) {
	ctx = setContextOp(ctx, aq.ctx, ent.OpQueryAll)
	if err := aq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Adventure, *AdventureQuery]()
	return withInterceptors[[]*Adventure](ctx, aq, qr, aq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aq *AdventureQuery) AllX(ctx context.Context) []*Adventure {
	nodes, err := aq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Adventure IDs.
func (aq *AdventureQuery) IDs(ctx context.Context) (ids []int, err error) {
	if aq.ctx.Unique == nil && aq.path != nil {
		aq.Unique(true)
	}
	ctx = setContextOp(ctx, aq.ctx, ent.OpQueryIDs)
	if err = aq.Select(adventure.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aq *AdventureQuery) IDsX(ctx context.Context) []int {
	ids, err := aq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aq *AdventureQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aq.ctx, ent.OpQueryCount)
	if err := aq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aq, querierCount[*AdventureQuery](), aq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aq *AdventureQuery) CountX(ctx context.Context) int {
	count, err := aq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aq *AdventureQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aq.ctx, ent.OpQueryExist)
	switch _, err := aq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aq *AdventureQuery) ExistX(ctx context.Context) bool {
	exist, err := aq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AdventureQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aq *AdventureQuery) Clone() *AdventureQuery {
	if aq == nil {
		return nil
	}
	return &AdventureQuery{
		config:     aq.config,
		ctx:        aq.ctx.Clone(),
		order:      append([]adventure.OrderOption{}, aq.order...),
		inters:     append([]Interceptor{}, aq.inters...),
		predicates: append([]predicate.Adventure{}, aq.predicates...),
		withQuests: aq.withQuests.Clone(),
		// clone intermediate query.
		sql:  aq.sql.Clone(),
		path: aq.path,
	}
}

// WithQuests tells the query-builder to eager-load the nodes that are connected to
// the "quests" edge. The optional arguments are used to configure the query builder of the edge.
func (aq *AdventureQuery) WithQuests(opts ...func(*QuestQuery)) *AdventureQuery {
	query := (&QuestClient{config: aq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aq.withQuests = query
	return aq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Adventure.Query().
//		GroupBy(adventure.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (aq *AdventureQuery) GroupBy(field string, fields ...string) *AdventureGroupBy {
	aq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AdventureGroupBy{build: aq}
	grbuild.flds = &aq.ctx.Fields
	grbuild.label = adventure.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Adventure.Query().
//		Select(adventure.FieldCreatedAt).
//		Scan(ctx, &v)
func (aq *AdventureQuery) Select(fields ...string) *AdventureSelect {
	aq.ctx.Fields = append(aq.ctx.Fields, fields...)
	sbuild := &AdventureSelect{AdventureQuery: aq}
	sbuild.label = adventure.Label
	sbuild.flds, sbuild.scan = &aq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AdventureSelect configured with the given aggregations.
func (aq *AdventureQuery) Aggregate(fns ...AggregateFunc) *AdventureSelect {
	return aq.Select().Aggregate(fns...)
}

func (aq *AdventureQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aq); err != nil {
				return err
			}
		}
	}
	for _, f := range aq.ctx.Fields {
		if !adventure.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aq.path != nil {
		prev, err := aq.path(ctx)
		if err != nil {
			return err
		}
		aq.sql = prev
	}
	return nil
}

func (aq *AdventureQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Adventure, error) {
	var (
		nodes       = []*Adventure{}
		_spec       = aq.querySpec()
		loadedTypes = [1]bool{
			aq.withQuests != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Adventure).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Adventure{config: aq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(aq.modifiers) > 0 {
		_spec.Modifiers = aq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := aq.withQuests; query != nil {
		if err := aq.loadQuests(ctx, query, nodes,
			func(n *Adventure) { n.Edges.Quests = []*Quest{} },
			func(n *Adventure, e *Quest) { n.Edges.Quests = append(n.Edges.Quests, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range aq.withNamedQuests {
		if err := aq.loadQuests(ctx, query, nodes,
			func(n *Adventure) { n.appendNamedQuests(name) },
			func(n *Adventure, e *Quest) { n.appendNamedQuests(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range aq.loadTotal {
		if err := aq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (aq *AdventureQuery) loadQuests(ctx context.Context, query *QuestQuery, nodes []*Adventure, init func(*Adventure), assign func(*Adventure, *Quest)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Adventure)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Quest(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(adventure.QuestsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.adventure_quests
		if fk == nil {
			return fmt.Errorf(`foreign-key "adventure_quests" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "adventure_quests" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (aq *AdventureQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aq.querySpec()
	if len(aq.modifiers) > 0 {
		_spec.Modifiers = aq.modifiers
	}
	_spec.Node.Columns = aq.ctx.Fields
	if len(aq.ctx.Fields) > 0 {
		_spec.Unique = aq.ctx.Unique != nil && *aq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, aq.driver, _spec)
}

func (aq *AdventureQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(adventure.Table, adventure.Columns, sqlgraph.NewFieldSpec(adventure.FieldID, field.TypeInt))
	_spec.From = aq.sql
	if unique := aq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aq.path != nil {
		_spec.Unique = true
	}
	if fields := aq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, adventure.FieldID)
		for i := range fields {
			if fields[i] != adventure.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := aq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aq *AdventureQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aq.driver.Dialect())
	t1 := builder.Table(adventure.Table)
	columns := aq.ctx.Fields
	if len(columns) == 0 {
		columns = adventure.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aq.sql != nil {
		selector = aq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aq.ctx.Unique != nil && *aq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range aq.predicates {
		p(selector)
	}
	for _, p := range aq.order {
		p(selector)
	}
	if offset := aq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedQuests tells the query-builder to eager-load the nodes that are connected to the "quests"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (aq *AdventureQuery) WithNamedQuests(name string, opts ...func(*QuestQuery)) *AdventureQuery {
	query := (&QuestClient{config: aq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if aq.withNamedQuests == nil {
		aq.withNamedQuests = make(map[string]*QuestQuery)
	}
	aq.withNamedQuests[name] = query
	return aq
}

// AdventureGroupBy is the group-by builder for Adventure entities.
type AdventureGroupBy struct {
	selector
	build *AdventureQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (agb *AdventureGroupBy) Aggregate(fns ...AggregateFunc) *AdventureGroupBy {
	agb.fns = append(agb.fns, fns...)
	return agb
}

// Scan applies the selector query and scans the result into the given value.
func (agb *AdventureGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, agb.build.ctx, ent.OpQueryGroupBy)
	if err := agb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AdventureQuery, *AdventureGroupBy](ctx, agb.build, agb, agb.build.inters, v)
}

func (agb *AdventureGroupBy) sqlScan(ctx context.Context, root *AdventureQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(agb.fns))
	for _, fn := range agb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*agb.flds)+len(agb.fns))
		for _, f := range *agb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*agb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := agb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AdventureSelect is the builder for selecting fields of Adventure entities.
type AdventureSelect struct {
	*AdventureQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (as *AdventureSelect) Aggregate(fns ...AggregateFunc) *AdventureSelect {
	as.fns = append(as.fns, fns...)
	return as
}

// Scan applies the selector query and scans the result into the given value.
func (as *AdventureSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, as.ctx, ent.OpQuerySelect)
	if err := as.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AdventureQuery, *AdventureSelect](ctx, as.AdventureQuery, as, as.inters, v)
}

func (as *AdventureSelect) sqlScan(ctx context.Context, root *AdventureQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(as.fns))
	for _, fn := range as.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*as.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := as.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
