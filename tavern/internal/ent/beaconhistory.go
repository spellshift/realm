// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"realm.pub/tavern/internal/ent/beacon"
	"realm.pub/tavern/internal/ent/beaconhistory"
)

// BeaconHistory is the model entity for the BeaconHistory schema.
type BeaconHistory struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Timestamp of when this ent was created
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Timestamp of when this ent was last updated
	LastModifiedAt time.Time `json:"last_modified_at,omitempty"`
	// The number of milliseconds off of its expected callback time. Can be negative if it checked-in ahead of schedule.
	Latency int64 `json:"latency,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BeaconHistoryQuery when eager-loading is set.
	Edges                 BeaconHistoryEdges `json:"edges"`
	beacon_history_beacon *int
	selectValues          sql.SelectValues
}

// BeaconHistoryEdges holds the relations/edges for other nodes in the graph.
type BeaconHistoryEdges struct {
	// The beacon this history belongs to.
	Beacon *Beacon `json:"beacon,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int
}

// BeaconOrErr returns the Beacon value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BeaconHistoryEdges) BeaconOrErr() (*Beacon, error) {
	if e.Beacon != nil {
		return e.Beacon, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: beacon.Label}
	}
	return nil, &NotLoadedError{edge: "beacon"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BeaconHistory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case beaconhistory.FieldID, beaconhistory.FieldLatency:
			values[i] = new(sql.NullInt64)
		case beaconhistory.FieldCreatedAt, beaconhistory.FieldLastModifiedAt:
			values[i] = new(sql.NullTime)
		case beaconhistory.ForeignKeys[0]: // beacon_history_beacon
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BeaconHistory fields.
func (bh *BeaconHistory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case beaconhistory.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			bh.ID = int(value.Int64)
		case beaconhistory.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				bh.CreatedAt = value.Time
			}
		case beaconhistory.FieldLastModifiedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_modified_at", values[i])
			} else if value.Valid {
				bh.LastModifiedAt = value.Time
			}
		case beaconhistory.FieldLatency:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field latency", values[i])
			} else if value.Valid {
				bh.Latency = value.Int64
			}
		case beaconhistory.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field beacon_history_beacon", value)
			} else if value.Valid {
				bh.beacon_history_beacon = new(int)
				*bh.beacon_history_beacon = int(value.Int64)
			}
		default:
			bh.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BeaconHistory.
// This includes values selected through modifiers, order, etc.
func (bh *BeaconHistory) Value(name string) (ent.Value, error) {
	return bh.selectValues.Get(name)
}

// QueryBeacon queries the "beacon" edge of the BeaconHistory entity.
func (bh *BeaconHistory) QueryBeacon() *BeaconQuery {
	return NewBeaconHistoryClient(bh.config).QueryBeacon(bh)
}

// Update returns a builder for updating this BeaconHistory.
// Note that you need to call BeaconHistory.Unwrap() before calling this method if this BeaconHistory
// was returned from a transaction, and the transaction was committed or rolled back.
func (bh *BeaconHistory) Update() *BeaconHistoryUpdateOne {
	return NewBeaconHistoryClient(bh.config).UpdateOne(bh)
}

// Unwrap unwraps the BeaconHistory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (bh *BeaconHistory) Unwrap() *BeaconHistory {
	_tx, ok := bh.config.driver.(*txDriver)
	if !ok {
		panic("ent: BeaconHistory is not a transactional entity")
	}
	bh.config.driver = _tx.drv
	return bh
}

// String implements the fmt.Stringer.
func (bh *BeaconHistory) String() string {
	var builder strings.Builder
	builder.WriteString("BeaconHistory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", bh.ID))
	builder.WriteString("created_at=")
	builder.WriteString(bh.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_modified_at=")
	builder.WriteString(bh.LastModifiedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("latency=")
	builder.WriteString(fmt.Sprintf("%v", bh.Latency))
	builder.WriteByte(')')
	return builder.String()
}

// BeaconHistories is a parsable slice of BeaconHistory.
type BeaconHistories []*BeaconHistory
