// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"realm.pub/tavern/internal/ent/deviceauth"
	"realm.pub/tavern/internal/ent/user"
)

// DeviceAuth is the model entity for the DeviceAuth schema.
type DeviceAuth struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Timestamp of when this ent was created
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Timestamp of when this ent was last updated
	LastModifiedAt time.Time `json:"last_modified_at,omitempty"`
	// Short code displayed to the user for auth.
	UserCode string `json:"user_code,omitempty"`
	// Long code used by the device to poll for auth status.
	DeviceCode string `json:"-"`
	// Status of the device authentication request.
	Status deviceauth.Status `json:"status,omitempty"`
	// When this device auth request expires.
	ExpiresAt time.Time `json:"expires_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DeviceAuthQuery when eager-loading is set.
	Edges            DeviceAuthEdges `json:"edges"`
	device_auth_user *int
	selectValues     sql.SelectValues
}

// DeviceAuthEdges holds the relations/edges for other nodes in the graph.
type DeviceAuthEdges struct {
	// User who approved the device auth (may be null if pending).
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DeviceAuthEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DeviceAuth) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case deviceauth.FieldID:
			values[i] = new(sql.NullInt64)
		case deviceauth.FieldUserCode, deviceauth.FieldDeviceCode, deviceauth.FieldStatus:
			values[i] = new(sql.NullString)
		case deviceauth.FieldCreatedAt, deviceauth.FieldLastModifiedAt, deviceauth.FieldExpiresAt:
			values[i] = new(sql.NullTime)
		case deviceauth.ForeignKeys[0]: // device_auth_user
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DeviceAuth fields.
func (da *DeviceAuth) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case deviceauth.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			da.ID = int(value.Int64)
		case deviceauth.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				da.CreatedAt = value.Time
			}
		case deviceauth.FieldLastModifiedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_modified_at", values[i])
			} else if value.Valid {
				da.LastModifiedAt = value.Time
			}
		case deviceauth.FieldUserCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_code", values[i])
			} else if value.Valid {
				da.UserCode = value.String
			}
		case deviceauth.FieldDeviceCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field device_code", values[i])
			} else if value.Valid {
				da.DeviceCode = value.String
			}
		case deviceauth.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				da.Status = deviceauth.Status(value.String)
			}
		case deviceauth.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				da.ExpiresAt = value.Time
			}
		case deviceauth.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field device_auth_user", value)
			} else if value.Valid {
				da.device_auth_user = new(int)
				*da.device_auth_user = int(value.Int64)
			}
		default:
			da.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DeviceAuth.
// This includes values selected through modifiers, order, etc.
func (da *DeviceAuth) Value(name string) (ent.Value, error) {
	return da.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the DeviceAuth entity.
func (da *DeviceAuth) QueryUser() *UserQuery {
	return NewDeviceAuthClient(da.config).QueryUser(da)
}

// Update returns a builder for updating this DeviceAuth.
// Note that you need to call DeviceAuth.Unwrap() before calling this method if this DeviceAuth
// was returned from a transaction, and the transaction was committed or rolled back.
func (da *DeviceAuth) Update() *DeviceAuthUpdateOne {
	return NewDeviceAuthClient(da.config).UpdateOne(da)
}

// Unwrap unwraps the DeviceAuth entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (da *DeviceAuth) Unwrap() *DeviceAuth {
	_tx, ok := da.config.driver.(*txDriver)
	if !ok {
		panic("ent: DeviceAuth is not a transactional entity")
	}
	da.config.driver = _tx.drv
	return da
}

// String implements the fmt.Stringer.
func (da *DeviceAuth) String() string {
	var builder strings.Builder
	builder.WriteString("DeviceAuth(")
	builder.WriteString(fmt.Sprintf("id=%v, ", da.ID))
	builder.WriteString("created_at=")
	builder.WriteString(da.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_modified_at=")
	builder.WriteString(da.LastModifiedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("user_code=")
	builder.WriteString(da.UserCode)
	builder.WriteString(", ")
	builder.WriteString("device_code=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", da.Status))
	builder.WriteString(", ")
	builder.WriteString("expires_at=")
	builder.WriteString(da.ExpiresAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DeviceAuths is a parsable slice of DeviceAuth.
type DeviceAuths []*DeviceAuth
