// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"realm.pub/tavern/internal/ent/host"
	"realm.pub/tavern/internal/ent/hostfact"
	"realm.pub/tavern/internal/ent/task"
)

// HostFact is the model entity for the HostFact schema.
type HostFact struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Timestamp of when this ent was created
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Timestamp of when this ent was last updated
	LastModifiedAt time.Time `json:"last_modified_at,omitempty"`
	// Name of the fact.
	Name string `json:"name,omitempty"`
	// Value of the fact.
	Value string `json:"value,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the HostFactQuery when eager-loading is set.
	Edges               HostFactEdges `json:"edges"`
	host_fact_host      *int
	task_reported_facts *int
	selectValues        sql.SelectValues
}

// HostFactEdges holds the relations/edges for other nodes in the graph.
type HostFactEdges struct {
	// Host the fact was reported on.
	Host *Host `json:"host,omitempty"`
	// Task that reported this fact.
	Task *Task `json:"task,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// HostOrErr returns the Host value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e HostFactEdges) HostOrErr() (*Host, error) {
	if e.Host != nil {
		return e.Host, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: host.Label}
	}
	return nil, &NotLoadedError{edge: "host"}
}

// TaskOrErr returns the Task value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e HostFactEdges) TaskOrErr() (*Task, error) {
	if e.Task != nil {
		return e.Task, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: task.Label}
	}
	return nil, &NotLoadedError{edge: "task"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*HostFact) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case hostfact.FieldID:
			values[i] = new(sql.NullInt64)
		case hostfact.FieldName, hostfact.FieldValue:
			values[i] = new(sql.NullString)
		case hostfact.FieldCreatedAt, hostfact.FieldLastModifiedAt:
			values[i] = new(sql.NullTime)
		case hostfact.ForeignKeys[0]: // host_fact_host
			values[i] = new(sql.NullInt64)
		case hostfact.ForeignKeys[1]: // task_reported_facts
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the HostFact fields.
func (hf *HostFact) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case hostfact.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			hf.ID = int(value.Int64)
		case hostfact.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				hf.CreatedAt = value.Time
			}
		case hostfact.FieldLastModifiedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_modified_at", values[i])
			} else if value.Valid {
				hf.LastModifiedAt = value.Time
			}
		case hostfact.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				hf.Name = value.String
			}
		case hostfact.FieldValue:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field value", values[i])
			} else if value.Valid {
				hf.Value = value.String
			}
		case hostfact.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field host_fact_host", value)
			} else if value.Valid {
				hf.host_fact_host = new(int)
				*hf.host_fact_host = int(value.Int64)
			}
		case hostfact.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field task_reported_facts", value)
			} else if value.Valid {
				hf.task_reported_facts = new(int)
				*hf.task_reported_facts = int(value.Int64)
			}
		default:
			hf.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// GetValue returns the ent.Value that was dynamically selected and assigned to the HostFact.
// This includes values selected through modifiers, order, etc.
func (hf *HostFact) GetValue(name string) (ent.Value, error) {
	return hf.selectValues.Get(name)
}

// QueryHost queries the "host" edge of the HostFact entity.
func (hf *HostFact) QueryHost() *HostQuery {
	return NewHostFactClient(hf.config).QueryHost(hf)
}

// QueryTask queries the "task" edge of the HostFact entity.
func (hf *HostFact) QueryTask() *TaskQuery {
	return NewHostFactClient(hf.config).QueryTask(hf)
}

// Update returns a builder for updating this HostFact.
// Note that you need to call HostFact.Unwrap() before calling this method if this HostFact
// was returned from a transaction, and the transaction was committed or rolled back.
func (hf *HostFact) Update() *HostFactUpdateOne {
	return NewHostFactClient(hf.config).UpdateOne(hf)
}

// Unwrap unwraps the HostFact entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (hf *HostFact) Unwrap() *HostFact {
	_tx, ok := hf.config.driver.(*txDriver)
	if !ok {
		panic("ent: HostFact is not a transactional entity")
	}
	hf.config.driver = _tx.drv
	return hf
}

// String implements the fmt.Stringer.
func (hf *HostFact) String() string {
	var builder strings.Builder
	builder.WriteString("HostFact(")
	builder.WriteString(fmt.Sprintf("id=%v, ", hf.ID))
	builder.WriteString("created_at=")
	builder.WriteString(hf.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_modified_at=")
	builder.WriteString(hf.LastModifiedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(hf.Name)
	builder.WriteString(", ")
	builder.WriteString("value=")
	builder.WriteString(hf.Value)
	builder.WriteByte(')')
	return builder.String()
}

// HostFacts is a parsable slice of HostFact.
type HostFacts []*HostFact
