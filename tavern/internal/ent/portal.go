// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"realm.pub/tavern/internal/ent/beacon"
	"realm.pub/tavern/internal/ent/portal"
	"realm.pub/tavern/internal/ent/shelltask"
	"realm.pub/tavern/internal/ent/task"
	"realm.pub/tavern/internal/ent/user"
)

// Portal is the model entity for the Portal schema.
type Portal struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Timestamp of when this ent was created
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Timestamp of when this ent was last updated
	LastModifiedAt time.Time `json:"last_modified_at,omitempty"`
	// Timestamp of when this portal was closed
	ClosedAt time.Time `json:"closed_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PortalQuery when eager-loading is set.
	Edges             PortalEdges `json:"edges"`
	portal_task       *int
	portal_shell_task *int
	portal_beacon     *int
	portal_owner      *int
	shell_portals     *int
	selectValues      sql.SelectValues
}

// PortalEdges holds the relations/edges for other nodes in the graph.
type PortalEdges struct {
	// Task that created the portal
	Task *Task `json:"task,omitempty"`
	// ShellTask that created the portal
	ShellTask *ShellTask `json:"shell_task,omitempty"`
	// Beacon that created the portal
	Beacon *Beacon `json:"beacon,omitempty"`
	// User that created the portal
	Owner *User `json:"owner,omitempty"`
	// Users that are currently using the portal
	ActiveUsers []*User `json:"active_users,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [5]bool
	// totalCount holds the count of the edges above.
	totalCount [5]map[string]int

	namedActiveUsers map[string][]*User
}

// TaskOrErr returns the Task value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PortalEdges) TaskOrErr() (*Task, error) {
	if e.Task != nil {
		return e.Task, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: task.Label}
	}
	return nil, &NotLoadedError{edge: "task"}
}

// ShellTaskOrErr returns the ShellTask value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PortalEdges) ShellTaskOrErr() (*ShellTask, error) {
	if e.ShellTask != nil {
		return e.ShellTask, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: shelltask.Label}
	}
	return nil, &NotLoadedError{edge: "shell_task"}
}

// BeaconOrErr returns the Beacon value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PortalEdges) BeaconOrErr() (*Beacon, error) {
	if e.Beacon != nil {
		return e.Beacon, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: beacon.Label}
	}
	return nil, &NotLoadedError{edge: "beacon"}
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PortalEdges) OwnerOrErr() (*User, error) {
	if e.Owner != nil {
		return e.Owner, nil
	} else if e.loadedTypes[3] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "owner"}
}

// ActiveUsersOrErr returns the ActiveUsers value or an error if the edge
// was not loaded in eager-loading.
func (e PortalEdges) ActiveUsersOrErr() ([]*User, error) {
	if e.loadedTypes[4] {
		return e.ActiveUsers, nil
	}
	return nil, &NotLoadedError{edge: "active_users"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Portal) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case portal.FieldID:
			values[i] = new(sql.NullInt64)
		case portal.FieldCreatedAt, portal.FieldLastModifiedAt, portal.FieldClosedAt:
			values[i] = new(sql.NullTime)
		case portal.ForeignKeys[0]: // portal_task
			values[i] = new(sql.NullInt64)
		case portal.ForeignKeys[1]: // portal_shell_task
			values[i] = new(sql.NullInt64)
		case portal.ForeignKeys[2]: // portal_beacon
			values[i] = new(sql.NullInt64)
		case portal.ForeignKeys[3]: // portal_owner
			values[i] = new(sql.NullInt64)
		case portal.ForeignKeys[4]: // shell_portals
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Portal fields.
func (po *Portal) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case portal.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			po.ID = int(value.Int64)
		case portal.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				po.CreatedAt = value.Time
			}
		case portal.FieldLastModifiedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_modified_at", values[i])
			} else if value.Valid {
				po.LastModifiedAt = value.Time
			}
		case portal.FieldClosedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field closed_at", values[i])
			} else if value.Valid {
				po.ClosedAt = value.Time
			}
		case portal.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field portal_task", value)
			} else if value.Valid {
				po.portal_task = new(int)
				*po.portal_task = int(value.Int64)
			}
		case portal.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field portal_shell_task", value)
			} else if value.Valid {
				po.portal_shell_task = new(int)
				*po.portal_shell_task = int(value.Int64)
			}
		case portal.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field portal_beacon", value)
			} else if value.Valid {
				po.portal_beacon = new(int)
				*po.portal_beacon = int(value.Int64)
			}
		case portal.ForeignKeys[3]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field portal_owner", value)
			} else if value.Valid {
				po.portal_owner = new(int)
				*po.portal_owner = int(value.Int64)
			}
		case portal.ForeignKeys[4]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field shell_portals", value)
			} else if value.Valid {
				po.shell_portals = new(int)
				*po.shell_portals = int(value.Int64)
			}
		default:
			po.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Portal.
// This includes values selected through modifiers, order, etc.
func (po *Portal) Value(name string) (ent.Value, error) {
	return po.selectValues.Get(name)
}

// QueryTask queries the "task" edge of the Portal entity.
func (po *Portal) QueryTask() *TaskQuery {
	return NewPortalClient(po.config).QueryTask(po)
}

// QueryShellTask queries the "shell_task" edge of the Portal entity.
func (po *Portal) QueryShellTask() *ShellTaskQuery {
	return NewPortalClient(po.config).QueryShellTask(po)
}

// QueryBeacon queries the "beacon" edge of the Portal entity.
func (po *Portal) QueryBeacon() *BeaconQuery {
	return NewPortalClient(po.config).QueryBeacon(po)
}

// QueryOwner queries the "owner" edge of the Portal entity.
func (po *Portal) QueryOwner() *UserQuery {
	return NewPortalClient(po.config).QueryOwner(po)
}

// QueryActiveUsers queries the "active_users" edge of the Portal entity.
func (po *Portal) QueryActiveUsers() *UserQuery {
	return NewPortalClient(po.config).QueryActiveUsers(po)
}

// Update returns a builder for updating this Portal.
// Note that you need to call Portal.Unwrap() before calling this method if this Portal
// was returned from a transaction, and the transaction was committed or rolled back.
func (po *Portal) Update() *PortalUpdateOne {
	return NewPortalClient(po.config).UpdateOne(po)
}

// Unwrap unwraps the Portal entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (po *Portal) Unwrap() *Portal {
	_tx, ok := po.config.driver.(*txDriver)
	if !ok {
		panic("ent: Portal is not a transactional entity")
	}
	po.config.driver = _tx.drv
	return po
}

// String implements the fmt.Stringer.
func (po *Portal) String() string {
	var builder strings.Builder
	builder.WriteString("Portal(")
	builder.WriteString(fmt.Sprintf("id=%v, ", po.ID))
	builder.WriteString("created_at=")
	builder.WriteString(po.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_modified_at=")
	builder.WriteString(po.LastModifiedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("closed_at=")
	builder.WriteString(po.ClosedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// NamedActiveUsers returns the ActiveUsers named value or an error if the edge was not
// loaded in eager-loading with this name.
func (po *Portal) NamedActiveUsers(name string) ([]*User, error) {
	if po.Edges.namedActiveUsers == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := po.Edges.namedActiveUsers[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (po *Portal) appendNamedActiveUsers(name string, edges ...*User) {
	if po.Edges.namedActiveUsers == nil {
		po.Edges.namedActiveUsers = make(map[string][]*User)
	}
	if len(edges) == 0 {
		po.Edges.namedActiveUsers[name] = []*User{}
	} else {
		po.Edges.namedActiveUsers[name] = append(po.Edges.namedActiveUsers[name], edges...)
	}
}

// Portals is a parsable slice of Portal.
type Portals []*Portal
