// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"realm.pub/tavern/internal/ent/host"
	"realm.pub/tavern/internal/ent/screenshot"
	"realm.pub/tavern/internal/ent/shelltask"
	"realm.pub/tavern/internal/ent/task"
)

// Screenshot is the model entity for the Screenshot schema.
type Screenshot struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Timestamp of when this ent was created
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Timestamp of when this ent was last updated
	LastModifiedAt time.Time `json:"last_modified_at,omitempty"`
	// Name of the screenshot file (e.g. screenshot_<hostname>_<timestamp>_<monitor>.png).
	Name string `json:"name,omitempty"`
	// The size of the screenshot in bytes
	Size uint64 `json:"size,omitempty"`
	// A SHA3-256 digest of the content field
	Hash string `json:"hash,omitempty"`
	// The content of the screenshot
	Content []byte `json:"content,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ScreenshotQuery when eager-loading is set.
	Edges                  ScreenshotEdges `json:"edges"`
	screenshot_host        *int
	shell_task_screenshots *int
	task_screenshots       *int
	selectValues           sql.SelectValues
}

// ScreenshotEdges holds the relations/edges for other nodes in the graph.
type ScreenshotEdges struct {
	// Host the screenshot was taken on.
	Host *Host `json:"host,omitempty"`
	// Task that reported this screenshot.
	Task *Task `json:"task,omitempty"`
	// Shell Task that reported this screenshot.
	ShellTask *ShellTask `json:"shell_task,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
	// totalCount holds the count of the edges above.
	totalCount [3]map[string]int
}

// HostOrErr returns the Host value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ScreenshotEdges) HostOrErr() (*Host, error) {
	if e.Host != nil {
		return e.Host, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: host.Label}
	}
	return nil, &NotLoadedError{edge: "host"}
}

// TaskOrErr returns the Task value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ScreenshotEdges) TaskOrErr() (*Task, error) {
	if e.Task != nil {
		return e.Task, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: task.Label}
	}
	return nil, &NotLoadedError{edge: "task"}
}

// ShellTaskOrErr returns the ShellTask value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ScreenshotEdges) ShellTaskOrErr() (*ShellTask, error) {
	if e.ShellTask != nil {
		return e.ShellTask, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: shelltask.Label}
	}
	return nil, &NotLoadedError{edge: "shell_task"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Screenshot) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case screenshot.FieldContent:
			values[i] = new([]byte)
		case screenshot.FieldID, screenshot.FieldSize:
			values[i] = new(sql.NullInt64)
		case screenshot.FieldName, screenshot.FieldHash:
			values[i] = new(sql.NullString)
		case screenshot.FieldCreatedAt, screenshot.FieldLastModifiedAt:
			values[i] = new(sql.NullTime)
		case screenshot.ForeignKeys[0]: // screenshot_host
			values[i] = new(sql.NullInt64)
		case screenshot.ForeignKeys[1]: // shell_task_screenshots
			values[i] = new(sql.NullInt64)
		case screenshot.ForeignKeys[2]: // task_screenshots
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Screenshot fields.
func (s *Screenshot) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case screenshot.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			s.ID = int(value.Int64)
		case screenshot.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				s.CreatedAt = value.Time
			}
		case screenshot.FieldLastModifiedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_modified_at", values[i])
			} else if value.Valid {
				s.LastModifiedAt = value.Time
			}
		case screenshot.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				s.Name = value.String
			}
		case screenshot.FieldSize:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field size", values[i])
			} else if value.Valid {
				s.Size = uint64(value.Int64)
			}
		case screenshot.FieldHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field hash", values[i])
			} else if value.Valid {
				s.Hash = value.String
			}
		case screenshot.FieldContent:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field content", values[i])
			} else if value != nil {
				s.Content = *value
			}
		case screenshot.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field screenshot_host", value)
			} else if value.Valid {
				s.screenshot_host = new(int)
				*s.screenshot_host = int(value.Int64)
			}
		case screenshot.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field shell_task_screenshots", value)
			} else if value.Valid {
				s.shell_task_screenshots = new(int)
				*s.shell_task_screenshots = int(value.Int64)
			}
		case screenshot.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field task_screenshots", value)
			} else if value.Valid {
				s.task_screenshots = new(int)
				*s.task_screenshots = int(value.Int64)
			}
		default:
			s.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Screenshot.
// This includes values selected through modifiers, order, etc.
func (s *Screenshot) Value(name string) (ent.Value, error) {
	return s.selectValues.Get(name)
}

// QueryHost queries the "host" edge of the Screenshot entity.
func (s *Screenshot) QueryHost() *HostQuery {
	return NewScreenshotClient(s.config).QueryHost(s)
}

// QueryTask queries the "task" edge of the Screenshot entity.
func (s *Screenshot) QueryTask() *TaskQuery {
	return NewScreenshotClient(s.config).QueryTask(s)
}

// QueryShellTask queries the "shell_task" edge of the Screenshot entity.
func (s *Screenshot) QueryShellTask() *ShellTaskQuery {
	return NewScreenshotClient(s.config).QueryShellTask(s)
}

// Update returns a builder for updating this Screenshot.
// Note that you need to call Screenshot.Unwrap() before calling this method if this Screenshot
// was returned from a transaction, and the transaction was committed or rolled back.
func (s *Screenshot) Update() *ScreenshotUpdateOne {
	return NewScreenshotClient(s.config).UpdateOne(s)
}

// Unwrap unwraps the Screenshot entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (s *Screenshot) Unwrap() *Screenshot {
	_tx, ok := s.config.driver.(*txDriver)
	if !ok {
		panic("ent: Screenshot is not a transactional entity")
	}
	s.config.driver = _tx.drv
	return s
}

// String implements the fmt.Stringer.
func (s *Screenshot) String() string {
	var builder strings.Builder
	builder.WriteString("Screenshot(")
	builder.WriteString(fmt.Sprintf("id=%v, ", s.ID))
	builder.WriteString("created_at=")
	builder.WriteString(s.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_modified_at=")
	builder.WriteString(s.LastModifiedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(s.Name)
	builder.WriteString(", ")
	builder.WriteString("size=")
	builder.WriteString(fmt.Sprintf("%v", s.Size))
	builder.WriteString(", ")
	builder.WriteString("hash=")
	builder.WriteString(s.Hash)
	builder.WriteString(", ")
	builder.WriteString("content=")
	builder.WriteString(fmt.Sprintf("%v", s.Content))
	builder.WriteByte(')')
	return builder.String()
}

// Screenshots is a parsable slice of Screenshot.
type Screenshots []*Screenshot
