// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"realm.pub/tavern/internal/ent/host"
	"realm.pub/tavern/internal/ent/predicate"
	"realm.pub/tavern/internal/ent/screenshot"
	"realm.pub/tavern/internal/ent/shelltask"
	"realm.pub/tavern/internal/ent/task"
)

// ScreenshotQuery is the builder for querying Screenshot entities.
type ScreenshotQuery struct {
	config
	ctx           *QueryContext
	order         []screenshot.OrderOption
	inters        []Interceptor
	predicates    []predicate.Screenshot
	withHost      *HostQuery
	withTask      *TaskQuery
	withShellTask *ShellTaskQuery
	withFKs       bool
	modifiers     []func(*sql.Selector)
	loadTotal     []func(context.Context, []*Screenshot) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ScreenshotQuery builder.
func (sq *ScreenshotQuery) Where(ps ...predicate.Screenshot) *ScreenshotQuery {
	sq.predicates = append(sq.predicates, ps...)
	return sq
}

// Limit the number of records to be returned by this query.
func (sq *ScreenshotQuery) Limit(limit int) *ScreenshotQuery {
	sq.ctx.Limit = &limit
	return sq
}

// Offset to start from.
func (sq *ScreenshotQuery) Offset(offset int) *ScreenshotQuery {
	sq.ctx.Offset = &offset
	return sq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sq *ScreenshotQuery) Unique(unique bool) *ScreenshotQuery {
	sq.ctx.Unique = &unique
	return sq
}

// Order specifies how the records should be ordered.
func (sq *ScreenshotQuery) Order(o ...screenshot.OrderOption) *ScreenshotQuery {
	sq.order = append(sq.order, o...)
	return sq
}

// QueryHost chains the current query on the "host" edge.
func (sq *ScreenshotQuery) QueryHost() *HostQuery {
	query := (&HostClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(screenshot.Table, screenshot.FieldID, selector),
			sqlgraph.To(host.Table, host.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, screenshot.HostTable, screenshot.HostColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTask chains the current query on the "task" edge.
func (sq *ScreenshotQuery) QueryTask() *TaskQuery {
	query := (&TaskClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(screenshot.Table, screenshot.FieldID, selector),
			sqlgraph.To(task.Table, task.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, screenshot.TaskTable, screenshot.TaskColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryShellTask chains the current query on the "shell_task" edge.
func (sq *ScreenshotQuery) QueryShellTask() *ShellTaskQuery {
	query := (&ShellTaskClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(screenshot.Table, screenshot.FieldID, selector),
			sqlgraph.To(shelltask.Table, shelltask.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, screenshot.ShellTaskTable, screenshot.ShellTaskColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Screenshot entity from the query.
// Returns a *NotFoundError when no Screenshot was found.
func (sq *ScreenshotQuery) First(ctx context.Context) (*Screenshot, error) {
	nodes, err := sq.Limit(1).All(setContextOp(ctx, sq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{screenshot.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sq *ScreenshotQuery) FirstX(ctx context.Context) *Screenshot {
	node, err := sq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Screenshot ID from the query.
// Returns a *NotFoundError when no Screenshot ID was found.
func (sq *ScreenshotQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sq.Limit(1).IDs(setContextOp(ctx, sq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{screenshot.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sq *ScreenshotQuery) FirstIDX(ctx context.Context) int {
	id, err := sq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Screenshot entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Screenshot entity is found.
// Returns a *NotFoundError when no Screenshot entities are found.
func (sq *ScreenshotQuery) Only(ctx context.Context) (*Screenshot, error) {
	nodes, err := sq.Limit(2).All(setContextOp(ctx, sq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{screenshot.Label}
	default:
		return nil, &NotSingularError{screenshot.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sq *ScreenshotQuery) OnlyX(ctx context.Context) *Screenshot {
	node, err := sq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Screenshot ID in the query.
// Returns a *NotSingularError when more than one Screenshot ID is found.
// Returns a *NotFoundError when no entities are found.
func (sq *ScreenshotQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sq.Limit(2).IDs(setContextOp(ctx, sq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{screenshot.Label}
	default:
		err = &NotSingularError{screenshot.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sq *ScreenshotQuery) OnlyIDX(ctx context.Context) int {
	id, err := sq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Screenshots.
func (sq *ScreenshotQuery) All(ctx context.Context) ([]*Screenshot, error) {
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryAll)
	if err := sq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Screenshot, *ScreenshotQuery]()
	return withInterceptors[[]*Screenshot](ctx, sq, qr, sq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sq *ScreenshotQuery) AllX(ctx context.Context) []*Screenshot {
	nodes, err := sq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Screenshot IDs.
func (sq *ScreenshotQuery) IDs(ctx context.Context) (ids []int, err error) {
	if sq.ctx.Unique == nil && sq.path != nil {
		sq.Unique(true)
	}
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryIDs)
	if err = sq.Select(screenshot.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sq *ScreenshotQuery) IDsX(ctx context.Context) []int {
	ids, err := sq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sq *ScreenshotQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryCount)
	if err := sq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sq, querierCount[*ScreenshotQuery](), sq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sq *ScreenshotQuery) CountX(ctx context.Context) int {
	count, err := sq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sq *ScreenshotQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryExist)
	switch _, err := sq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sq *ScreenshotQuery) ExistX(ctx context.Context) bool {
	exist, err := sq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ScreenshotQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sq *ScreenshotQuery) Clone() *ScreenshotQuery {
	if sq == nil {
		return nil
	}
	return &ScreenshotQuery{
		config:        sq.config,
		ctx:           sq.ctx.Clone(),
		order:         append([]screenshot.OrderOption{}, sq.order...),
		inters:        append([]Interceptor{}, sq.inters...),
		predicates:    append([]predicate.Screenshot{}, sq.predicates...),
		withHost:      sq.withHost.Clone(),
		withTask:      sq.withTask.Clone(),
		withShellTask: sq.withShellTask.Clone(),
		// clone intermediate query.
		sql:  sq.sql.Clone(),
		path: sq.path,
	}
}

// WithHost tells the query-builder to eager-load the nodes that are connected to
// the "host" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *ScreenshotQuery) WithHost(opts ...func(*HostQuery)) *ScreenshotQuery {
	query := (&HostClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withHost = query
	return sq
}

// WithTask tells the query-builder to eager-load the nodes that are connected to
// the "task" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *ScreenshotQuery) WithTask(opts ...func(*TaskQuery)) *ScreenshotQuery {
	query := (&TaskClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withTask = query
	return sq
}

// WithShellTask tells the query-builder to eager-load the nodes that are connected to
// the "shell_task" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *ScreenshotQuery) WithShellTask(opts ...func(*ShellTaskQuery)) *ScreenshotQuery {
	query := (&ShellTaskClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withShellTask = query
	return sq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Screenshot.Query().
//		GroupBy(screenshot.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sq *ScreenshotQuery) GroupBy(field string, fields ...string) *ScreenshotGroupBy {
	sq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ScreenshotGroupBy{build: sq}
	grbuild.flds = &sq.ctx.Fields
	grbuild.label = screenshot.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Screenshot.Query().
//		Select(screenshot.FieldCreatedAt).
//		Scan(ctx, &v)
func (sq *ScreenshotQuery) Select(fields ...string) *ScreenshotSelect {
	sq.ctx.Fields = append(sq.ctx.Fields, fields...)
	sbuild := &ScreenshotSelect{ScreenshotQuery: sq}
	sbuild.label = screenshot.Label
	sbuild.flds, sbuild.scan = &sq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ScreenshotSelect configured with the given aggregations.
func (sq *ScreenshotQuery) Aggregate(fns ...AggregateFunc) *ScreenshotSelect {
	return sq.Select().Aggregate(fns...)
}

func (sq *ScreenshotQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sq); err != nil {
				return err
			}
		}
	}
	for _, f := range sq.ctx.Fields {
		if !screenshot.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sq.path != nil {
		prev, err := sq.path(ctx)
		if err != nil {
			return err
		}
		sq.sql = prev
	}
	return nil
}

func (sq *ScreenshotQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Screenshot, error) {
	var (
		nodes       = []*Screenshot{}
		withFKs     = sq.withFKs
		_spec       = sq.querySpec()
		loadedTypes = [3]bool{
			sq.withHost != nil,
			sq.withTask != nil,
			sq.withShellTask != nil,
		}
	)
	if sq.withHost != nil || sq.withTask != nil || sq.withShellTask != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, screenshot.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Screenshot).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Screenshot{config: sq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(sq.modifiers) > 0 {
		_spec.Modifiers = sq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sq.withHost; query != nil {
		if err := sq.loadHost(ctx, query, nodes, nil,
			func(n *Screenshot, e *Host) { n.Edges.Host = e }); err != nil {
			return nil, err
		}
	}
	if query := sq.withTask; query != nil {
		if err := sq.loadTask(ctx, query, nodes, nil,
			func(n *Screenshot, e *Task) { n.Edges.Task = e }); err != nil {
			return nil, err
		}
	}
	if query := sq.withShellTask; query != nil {
		if err := sq.loadShellTask(ctx, query, nodes, nil,
			func(n *Screenshot, e *ShellTask) { n.Edges.ShellTask = e }); err != nil {
			return nil, err
		}
	}
	for i := range sq.loadTotal {
		if err := sq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sq *ScreenshotQuery) loadHost(ctx context.Context, query *HostQuery, nodes []*Screenshot, init func(*Screenshot), assign func(*Screenshot, *Host)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Screenshot)
	for i := range nodes {
		if nodes[i].screenshot_host == nil {
			continue
		}
		fk := *nodes[i].screenshot_host
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(host.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "screenshot_host" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (sq *ScreenshotQuery) loadTask(ctx context.Context, query *TaskQuery, nodes []*Screenshot, init func(*Screenshot), assign func(*Screenshot, *Task)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Screenshot)
	for i := range nodes {
		if nodes[i].task_screenshots == nil {
			continue
		}
		fk := *nodes[i].task_screenshots
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(task.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "task_screenshots" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (sq *ScreenshotQuery) loadShellTask(ctx context.Context, query *ShellTaskQuery, nodes []*Screenshot, init func(*Screenshot), assign func(*Screenshot, *ShellTask)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Screenshot)
	for i := range nodes {
		if nodes[i].shell_task_screenshots == nil {
			continue
		}
		fk := *nodes[i].shell_task_screenshots
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(shelltask.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "shell_task_screenshots" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (sq *ScreenshotQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sq.querySpec()
	if len(sq.modifiers) > 0 {
		_spec.Modifiers = sq.modifiers
	}
	_spec.Node.Columns = sq.ctx.Fields
	if len(sq.ctx.Fields) > 0 {
		_spec.Unique = sq.ctx.Unique != nil && *sq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sq.driver, _spec)
}

func (sq *ScreenshotQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(screenshot.Table, screenshot.Columns, sqlgraph.NewFieldSpec(screenshot.FieldID, field.TypeInt))
	_spec.From = sq.sql
	if unique := sq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sq.path != nil {
		_spec.Unique = true
	}
	if fields := sq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, screenshot.FieldID)
		for i := range fields {
			if fields[i] != screenshot.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sq *ScreenshotQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sq.driver.Dialect())
	t1 := builder.Table(screenshot.Table)
	columns := sq.ctx.Fields
	if len(columns) == 0 {
		columns = screenshot.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sq.sql != nil {
		selector = sq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sq.ctx.Unique != nil && *sq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sq.predicates {
		p(selector)
	}
	for _, p := range sq.order {
		p(selector)
	}
	if offset := sq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ScreenshotGroupBy is the group-by builder for Screenshot entities.
type ScreenshotGroupBy struct {
	selector
	build *ScreenshotQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sgb *ScreenshotGroupBy) Aggregate(fns ...AggregateFunc) *ScreenshotGroupBy {
	sgb.fns = append(sgb.fns, fns...)
	return sgb
}

// Scan applies the selector query and scans the result into the given value.
func (sgb *ScreenshotGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sgb.build.ctx, ent.OpQueryGroupBy)
	if err := sgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ScreenshotQuery, *ScreenshotGroupBy](ctx, sgb.build, sgb, sgb.build.inters, v)
}

func (sgb *ScreenshotGroupBy) sqlScan(ctx context.Context, root *ScreenshotQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sgb.fns))
	for _, fn := range sgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sgb.flds)+len(sgb.fns))
		for _, f := range *sgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ScreenshotSelect is the builder for selecting fields of Screenshot entities.
type ScreenshotSelect struct {
	*ScreenshotQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ss *ScreenshotSelect) Aggregate(fns ...AggregateFunc) *ScreenshotSelect {
	ss.fns = append(ss.fns, fns...)
	return ss
}

// Scan applies the selector query and scans the result into the given value.
func (ss *ScreenshotSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ss.ctx, ent.OpQuerySelect)
	if err := ss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ScreenshotQuery, *ScreenshotSelect](ctx, ss.ScreenshotQuery, ss, ss.inters, v)
}

func (ss *ScreenshotSelect) sqlScan(ctx context.Context, root *ScreenshotQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ss.fns))
	for _, fn := range ss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
