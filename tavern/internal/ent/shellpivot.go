// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"realm.pub/tavern/internal/ent/hostcredential"
	"realm.pub/tavern/internal/ent/portal"
	"realm.pub/tavern/internal/ent/shell"
	"realm.pub/tavern/internal/ent/shellpivot"
)

// ShellPivot is the model entity for the ShellPivot schema.
type ShellPivot struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Timestamp of when this ent was created
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Timestamp of when this ent was last updated
	LastModifiedAt time.Time `json:"last_modified_at,omitempty"`
	// Timestamp of when this pivot was closed
	ClosedAt time.Time `json:"closed_at,omitempty"`
	// Stream ID of the portal connection
	StreamID string `json:"stream_id,omitempty"`
	// Kind of pivot connection
	Kind shellpivot.Kind `json:"kind,omitempty"`
	// Destination address of the pivot
	Destination string `json:"destination,omitempty"`
	// Destination port of the pivot
	Port int `json:"port,omitempty"`
	// Pivot data stream buffer
	Data string `json:"data,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShellPivotQuery when eager-loading is set.
	Edges                  ShellPivotEdges `json:"edges"`
	shell_pivot_shell      *int
	shell_pivot_portal     *int
	shell_pivot_credential *int
	selectValues           sql.SelectValues
}

// ShellPivotEdges holds the relations/edges for other nodes in the graph.
type ShellPivotEdges struct {
	// Shell associated with this pivot
	Shell *Shell `json:"shell,omitempty"`
	// Portal associated with this pivot
	Portal *Portal `json:"portal,omitempty"`
	// Credential used for this pivot
	Credential *HostCredential `json:"credential,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
	// totalCount holds the count of the edges above.
	totalCount [3]map[string]int
}

// ShellOrErr returns the Shell value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShellPivotEdges) ShellOrErr() (*Shell, error) {
	if e.Shell != nil {
		return e.Shell, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: shell.Label}
	}
	return nil, &NotLoadedError{edge: "shell"}
}

// PortalOrErr returns the Portal value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShellPivotEdges) PortalOrErr() (*Portal, error) {
	if e.Portal != nil {
		return e.Portal, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: portal.Label}
	}
	return nil, &NotLoadedError{edge: "portal"}
}

// CredentialOrErr returns the Credential value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShellPivotEdges) CredentialOrErr() (*HostCredential, error) {
	if e.Credential != nil {
		return e.Credential, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: hostcredential.Label}
	}
	return nil, &NotLoadedError{edge: "credential"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ShellPivot) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case shellpivot.FieldID, shellpivot.FieldPort:
			values[i] = new(sql.NullInt64)
		case shellpivot.FieldStreamID, shellpivot.FieldKind, shellpivot.FieldDestination, shellpivot.FieldData:
			values[i] = new(sql.NullString)
		case shellpivot.FieldCreatedAt, shellpivot.FieldLastModifiedAt, shellpivot.FieldClosedAt:
			values[i] = new(sql.NullTime)
		case shellpivot.ForeignKeys[0]: // shell_pivot_shell
			values[i] = new(sql.NullInt64)
		case shellpivot.ForeignKeys[1]: // shell_pivot_portal
			values[i] = new(sql.NullInt64)
		case shellpivot.ForeignKeys[2]: // shell_pivot_credential
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ShellPivot fields.
func (sp *ShellPivot) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case shellpivot.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sp.ID = int(value.Int64)
		case shellpivot.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sp.CreatedAt = value.Time
			}
		case shellpivot.FieldLastModifiedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_modified_at", values[i])
			} else if value.Valid {
				sp.LastModifiedAt = value.Time
			}
		case shellpivot.FieldClosedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field closed_at", values[i])
			} else if value.Valid {
				sp.ClosedAt = value.Time
			}
		case shellpivot.FieldStreamID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field stream_id", values[i])
			} else if value.Valid {
				sp.StreamID = value.String
			}
		case shellpivot.FieldKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field kind", values[i])
			} else if value.Valid {
				sp.Kind = shellpivot.Kind(value.String)
			}
		case shellpivot.FieldDestination:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field destination", values[i])
			} else if value.Valid {
				sp.Destination = value.String
			}
		case shellpivot.FieldPort:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field port", values[i])
			} else if value.Valid {
				sp.Port = int(value.Int64)
			}
		case shellpivot.FieldData:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field data", values[i])
			} else if value.Valid {
				sp.Data = value.String
			}
		case shellpivot.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field shell_pivot_shell", value)
			} else if value.Valid {
				sp.shell_pivot_shell = new(int)
				*sp.shell_pivot_shell = int(value.Int64)
			}
		case shellpivot.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field shell_pivot_portal", value)
			} else if value.Valid {
				sp.shell_pivot_portal = new(int)
				*sp.shell_pivot_portal = int(value.Int64)
			}
		case shellpivot.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field shell_pivot_credential", value)
			} else if value.Valid {
				sp.shell_pivot_credential = new(int)
				*sp.shell_pivot_credential = int(value.Int64)
			}
		default:
			sp.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ShellPivot.
// This includes values selected through modifiers, order, etc.
func (sp *ShellPivot) Value(name string) (ent.Value, error) {
	return sp.selectValues.Get(name)
}

// QueryShell queries the "shell" edge of the ShellPivot entity.
func (sp *ShellPivot) QueryShell() *ShellQuery {
	return NewShellPivotClient(sp.config).QueryShell(sp)
}

// QueryPortal queries the "portal" edge of the ShellPivot entity.
func (sp *ShellPivot) QueryPortal() *PortalQuery {
	return NewShellPivotClient(sp.config).QueryPortal(sp)
}

// QueryCredential queries the "credential" edge of the ShellPivot entity.
func (sp *ShellPivot) QueryCredential() *HostCredentialQuery {
	return NewShellPivotClient(sp.config).QueryCredential(sp)
}

// Update returns a builder for updating this ShellPivot.
// Note that you need to call ShellPivot.Unwrap() before calling this method if this ShellPivot
// was returned from a transaction, and the transaction was committed or rolled back.
func (sp *ShellPivot) Update() *ShellPivotUpdateOne {
	return NewShellPivotClient(sp.config).UpdateOne(sp)
}

// Unwrap unwraps the ShellPivot entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sp *ShellPivot) Unwrap() *ShellPivot {
	_tx, ok := sp.config.driver.(*txDriver)
	if !ok {
		panic("ent: ShellPivot is not a transactional entity")
	}
	sp.config.driver = _tx.drv
	return sp
}

// String implements the fmt.Stringer.
func (sp *ShellPivot) String() string {
	var builder strings.Builder
	builder.WriteString("ShellPivot(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sp.ID))
	builder.WriteString("created_at=")
	builder.WriteString(sp.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_modified_at=")
	builder.WriteString(sp.LastModifiedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("closed_at=")
	builder.WriteString(sp.ClosedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("stream_id=")
	builder.WriteString(sp.StreamID)
	builder.WriteString(", ")
	builder.WriteString("kind=")
	builder.WriteString(fmt.Sprintf("%v", sp.Kind))
	builder.WriteString(", ")
	builder.WriteString("destination=")
	builder.WriteString(sp.Destination)
	builder.WriteString(", ")
	builder.WriteString("port=")
	builder.WriteString(fmt.Sprintf("%v", sp.Port))
	builder.WriteString(", ")
	builder.WriteString("data=")
	builder.WriteString(sp.Data)
	builder.WriteByte(')')
	return builder.String()
}

// ShellPivots is a parsable slice of ShellPivot.
type ShellPivots []*ShellPivot
