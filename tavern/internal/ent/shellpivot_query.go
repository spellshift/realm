// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"realm.pub/tavern/internal/ent/hostcredential"
	"realm.pub/tavern/internal/ent/portal"
	"realm.pub/tavern/internal/ent/predicate"
	"realm.pub/tavern/internal/ent/shell"
	"realm.pub/tavern/internal/ent/shellpivot"
)

// ShellPivotQuery is the builder for querying ShellPivot entities.
type ShellPivotQuery struct {
	config
	ctx            *QueryContext
	order          []shellpivot.OrderOption
	inters         []Interceptor
	predicates     []predicate.ShellPivot
	withShell      *ShellQuery
	withPortal     *PortalQuery
	withCredential *HostCredentialQuery
	withFKs        bool
	modifiers      []func(*sql.Selector)
	loadTotal      []func(context.Context, []*ShellPivot) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ShellPivotQuery builder.
func (spq *ShellPivotQuery) Where(ps ...predicate.ShellPivot) *ShellPivotQuery {
	spq.predicates = append(spq.predicates, ps...)
	return spq
}

// Limit the number of records to be returned by this query.
func (spq *ShellPivotQuery) Limit(limit int) *ShellPivotQuery {
	spq.ctx.Limit = &limit
	return spq
}

// Offset to start from.
func (spq *ShellPivotQuery) Offset(offset int) *ShellPivotQuery {
	spq.ctx.Offset = &offset
	return spq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (spq *ShellPivotQuery) Unique(unique bool) *ShellPivotQuery {
	spq.ctx.Unique = &unique
	return spq
}

// Order specifies how the records should be ordered.
func (spq *ShellPivotQuery) Order(o ...shellpivot.OrderOption) *ShellPivotQuery {
	spq.order = append(spq.order, o...)
	return spq
}

// QueryShell chains the current query on the "shell" edge.
func (spq *ShellPivotQuery) QueryShell() *ShellQuery {
	query := (&ShellClient{config: spq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := spq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := spq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shellpivot.Table, shellpivot.FieldID, selector),
			sqlgraph.To(shell.Table, shell.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, shellpivot.ShellTable, shellpivot.ShellColumn),
		)
		fromU = sqlgraph.SetNeighbors(spq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPortal chains the current query on the "portal" edge.
func (spq *ShellPivotQuery) QueryPortal() *PortalQuery {
	query := (&PortalClient{config: spq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := spq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := spq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shellpivot.Table, shellpivot.FieldID, selector),
			sqlgraph.To(portal.Table, portal.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, shellpivot.PortalTable, shellpivot.PortalColumn),
		)
		fromU = sqlgraph.SetNeighbors(spq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCredential chains the current query on the "credential" edge.
func (spq *ShellPivotQuery) QueryCredential() *HostCredentialQuery {
	query := (&HostCredentialClient{config: spq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := spq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := spq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shellpivot.Table, shellpivot.FieldID, selector),
			sqlgraph.To(hostcredential.Table, hostcredential.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, shellpivot.CredentialTable, shellpivot.CredentialColumn),
		)
		fromU = sqlgraph.SetNeighbors(spq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ShellPivot entity from the query.
// Returns a *NotFoundError when no ShellPivot was found.
func (spq *ShellPivotQuery) First(ctx context.Context) (*ShellPivot, error) {
	nodes, err := spq.Limit(1).All(setContextOp(ctx, spq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{shellpivot.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (spq *ShellPivotQuery) FirstX(ctx context.Context) *ShellPivot {
	node, err := spq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ShellPivot ID from the query.
// Returns a *NotFoundError when no ShellPivot ID was found.
func (spq *ShellPivotQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = spq.Limit(1).IDs(setContextOp(ctx, spq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{shellpivot.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (spq *ShellPivotQuery) FirstIDX(ctx context.Context) int {
	id, err := spq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ShellPivot entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ShellPivot entity is found.
// Returns a *NotFoundError when no ShellPivot entities are found.
func (spq *ShellPivotQuery) Only(ctx context.Context) (*ShellPivot, error) {
	nodes, err := spq.Limit(2).All(setContextOp(ctx, spq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{shellpivot.Label}
	default:
		return nil, &NotSingularError{shellpivot.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (spq *ShellPivotQuery) OnlyX(ctx context.Context) *ShellPivot {
	node, err := spq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ShellPivot ID in the query.
// Returns a *NotSingularError when more than one ShellPivot ID is found.
// Returns a *NotFoundError when no entities are found.
func (spq *ShellPivotQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = spq.Limit(2).IDs(setContextOp(ctx, spq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{shellpivot.Label}
	default:
		err = &NotSingularError{shellpivot.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (spq *ShellPivotQuery) OnlyIDX(ctx context.Context) int {
	id, err := spq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ShellPivots.
func (spq *ShellPivotQuery) All(ctx context.Context) ([]*ShellPivot, error) {
	ctx = setContextOp(ctx, spq.ctx, ent.OpQueryAll)
	if err := spq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ShellPivot, *ShellPivotQuery]()
	return withInterceptors[[]*ShellPivot](ctx, spq, qr, spq.inters)
}

// AllX is like All, but panics if an error occurs.
func (spq *ShellPivotQuery) AllX(ctx context.Context) []*ShellPivot {
	nodes, err := spq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ShellPivot IDs.
func (spq *ShellPivotQuery) IDs(ctx context.Context) (ids []int, err error) {
	if spq.ctx.Unique == nil && spq.path != nil {
		spq.Unique(true)
	}
	ctx = setContextOp(ctx, spq.ctx, ent.OpQueryIDs)
	if err = spq.Select(shellpivot.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (spq *ShellPivotQuery) IDsX(ctx context.Context) []int {
	ids, err := spq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (spq *ShellPivotQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, spq.ctx, ent.OpQueryCount)
	if err := spq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, spq, querierCount[*ShellPivotQuery](), spq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (spq *ShellPivotQuery) CountX(ctx context.Context) int {
	count, err := spq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (spq *ShellPivotQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, spq.ctx, ent.OpQueryExist)
	switch _, err := spq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (spq *ShellPivotQuery) ExistX(ctx context.Context) bool {
	exist, err := spq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ShellPivotQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (spq *ShellPivotQuery) Clone() *ShellPivotQuery {
	if spq == nil {
		return nil
	}
	return &ShellPivotQuery{
		config:         spq.config,
		ctx:            spq.ctx.Clone(),
		order:          append([]shellpivot.OrderOption{}, spq.order...),
		inters:         append([]Interceptor{}, spq.inters...),
		predicates:     append([]predicate.ShellPivot{}, spq.predicates...),
		withShell:      spq.withShell.Clone(),
		withPortal:     spq.withPortal.Clone(),
		withCredential: spq.withCredential.Clone(),
		// clone intermediate query.
		sql:  spq.sql.Clone(),
		path: spq.path,
	}
}

// WithShell tells the query-builder to eager-load the nodes that are connected to
// the "shell" edge. The optional arguments are used to configure the query builder of the edge.
func (spq *ShellPivotQuery) WithShell(opts ...func(*ShellQuery)) *ShellPivotQuery {
	query := (&ShellClient{config: spq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	spq.withShell = query
	return spq
}

// WithPortal tells the query-builder to eager-load the nodes that are connected to
// the "portal" edge. The optional arguments are used to configure the query builder of the edge.
func (spq *ShellPivotQuery) WithPortal(opts ...func(*PortalQuery)) *ShellPivotQuery {
	query := (&PortalClient{config: spq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	spq.withPortal = query
	return spq
}

// WithCredential tells the query-builder to eager-load the nodes that are connected to
// the "credential" edge. The optional arguments are used to configure the query builder of the edge.
func (spq *ShellPivotQuery) WithCredential(opts ...func(*HostCredentialQuery)) *ShellPivotQuery {
	query := (&HostCredentialClient{config: spq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	spq.withCredential = query
	return spq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ShellPivot.Query().
//		GroupBy(shellpivot.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (spq *ShellPivotQuery) GroupBy(field string, fields ...string) *ShellPivotGroupBy {
	spq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ShellPivotGroupBy{build: spq}
	grbuild.flds = &spq.ctx.Fields
	grbuild.label = shellpivot.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ShellPivot.Query().
//		Select(shellpivot.FieldCreatedAt).
//		Scan(ctx, &v)
func (spq *ShellPivotQuery) Select(fields ...string) *ShellPivotSelect {
	spq.ctx.Fields = append(spq.ctx.Fields, fields...)
	sbuild := &ShellPivotSelect{ShellPivotQuery: spq}
	sbuild.label = shellpivot.Label
	sbuild.flds, sbuild.scan = &spq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ShellPivotSelect configured with the given aggregations.
func (spq *ShellPivotQuery) Aggregate(fns ...AggregateFunc) *ShellPivotSelect {
	return spq.Select().Aggregate(fns...)
}

func (spq *ShellPivotQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range spq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, spq); err != nil {
				return err
			}
		}
	}
	for _, f := range spq.ctx.Fields {
		if !shellpivot.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if spq.path != nil {
		prev, err := spq.path(ctx)
		if err != nil {
			return err
		}
		spq.sql = prev
	}
	return nil
}

func (spq *ShellPivotQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ShellPivot, error) {
	var (
		nodes       = []*ShellPivot{}
		withFKs     = spq.withFKs
		_spec       = spq.querySpec()
		loadedTypes = [3]bool{
			spq.withShell != nil,
			spq.withPortal != nil,
			spq.withCredential != nil,
		}
	)
	if spq.withShell != nil || spq.withPortal != nil || spq.withCredential != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, shellpivot.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ShellPivot).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ShellPivot{config: spq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(spq.modifiers) > 0 {
		_spec.Modifiers = spq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, spq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := spq.withShell; query != nil {
		if err := spq.loadShell(ctx, query, nodes, nil,
			func(n *ShellPivot, e *Shell) { n.Edges.Shell = e }); err != nil {
			return nil, err
		}
	}
	if query := spq.withPortal; query != nil {
		if err := spq.loadPortal(ctx, query, nodes, nil,
			func(n *ShellPivot, e *Portal) { n.Edges.Portal = e }); err != nil {
			return nil, err
		}
	}
	if query := spq.withCredential; query != nil {
		if err := spq.loadCredential(ctx, query, nodes, nil,
			func(n *ShellPivot, e *HostCredential) { n.Edges.Credential = e }); err != nil {
			return nil, err
		}
	}
	for i := range spq.loadTotal {
		if err := spq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (spq *ShellPivotQuery) loadShell(ctx context.Context, query *ShellQuery, nodes []*ShellPivot, init func(*ShellPivot), assign func(*ShellPivot, *Shell)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ShellPivot)
	for i := range nodes {
		if nodes[i].shell_pivot_shell == nil {
			continue
		}
		fk := *nodes[i].shell_pivot_shell
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(shell.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "shell_pivot_shell" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (spq *ShellPivotQuery) loadPortal(ctx context.Context, query *PortalQuery, nodes []*ShellPivot, init func(*ShellPivot), assign func(*ShellPivot, *Portal)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ShellPivot)
	for i := range nodes {
		if nodes[i].shell_pivot_portal == nil {
			continue
		}
		fk := *nodes[i].shell_pivot_portal
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(portal.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "shell_pivot_portal" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (spq *ShellPivotQuery) loadCredential(ctx context.Context, query *HostCredentialQuery, nodes []*ShellPivot, init func(*ShellPivot), assign func(*ShellPivot, *HostCredential)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ShellPivot)
	for i := range nodes {
		if nodes[i].shell_pivot_credential == nil {
			continue
		}
		fk := *nodes[i].shell_pivot_credential
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(hostcredential.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "shell_pivot_credential" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (spq *ShellPivotQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := spq.querySpec()
	if len(spq.modifiers) > 0 {
		_spec.Modifiers = spq.modifiers
	}
	_spec.Node.Columns = spq.ctx.Fields
	if len(spq.ctx.Fields) > 0 {
		_spec.Unique = spq.ctx.Unique != nil && *spq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, spq.driver, _spec)
}

func (spq *ShellPivotQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(shellpivot.Table, shellpivot.Columns, sqlgraph.NewFieldSpec(shellpivot.FieldID, field.TypeInt))
	_spec.From = spq.sql
	if unique := spq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if spq.path != nil {
		_spec.Unique = true
	}
	if fields := spq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, shellpivot.FieldID)
		for i := range fields {
			if fields[i] != shellpivot.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := spq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := spq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := spq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := spq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (spq *ShellPivotQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(spq.driver.Dialect())
	t1 := builder.Table(shellpivot.Table)
	columns := spq.ctx.Fields
	if len(columns) == 0 {
		columns = shellpivot.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if spq.sql != nil {
		selector = spq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if spq.ctx.Unique != nil && *spq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range spq.predicates {
		p(selector)
	}
	for _, p := range spq.order {
		p(selector)
	}
	if offset := spq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := spq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ShellPivotGroupBy is the group-by builder for ShellPivot entities.
type ShellPivotGroupBy struct {
	selector
	build *ShellPivotQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (spgb *ShellPivotGroupBy) Aggregate(fns ...AggregateFunc) *ShellPivotGroupBy {
	spgb.fns = append(spgb.fns, fns...)
	return spgb
}

// Scan applies the selector query and scans the result into the given value.
func (spgb *ShellPivotGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, spgb.build.ctx, ent.OpQueryGroupBy)
	if err := spgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShellPivotQuery, *ShellPivotGroupBy](ctx, spgb.build, spgb, spgb.build.inters, v)
}

func (spgb *ShellPivotGroupBy) sqlScan(ctx context.Context, root *ShellPivotQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(spgb.fns))
	for _, fn := range spgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*spgb.flds)+len(spgb.fns))
		for _, f := range *spgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*spgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := spgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ShellPivotSelect is the builder for selecting fields of ShellPivot entities.
type ShellPivotSelect struct {
	*ShellPivotQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sps *ShellPivotSelect) Aggregate(fns ...AggregateFunc) *ShellPivotSelect {
	sps.fns = append(sps.fns, fns...)
	return sps
}

// Scan applies the selector query and scans the result into the given value.
func (sps *ShellPivotSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sps.ctx, ent.OpQuerySelect)
	if err := sps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShellPivotQuery, *ShellPivotSelect](ctx, sps.ShellPivotQuery, sps, sps.inters, v)
}

func (sps *ShellPivotSelect) sqlScan(ctx context.Context, root *ShellPivotQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sps.fns))
	for _, fn := range sps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
