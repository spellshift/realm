// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"realm.pub/tavern/internal/ent/hostcredential"
	"realm.pub/tavern/internal/ent/hostfile"
	"realm.pub/tavern/internal/ent/hostprocess"
	"realm.pub/tavern/internal/ent/predicate"
	"realm.pub/tavern/internal/ent/shell"
	"realm.pub/tavern/internal/ent/shelltask"
	"realm.pub/tavern/internal/ent/user"
)

// ShellTaskQuery is the builder for querying ShellTask entities.
type ShellTaskQuery struct {
	config
	ctx                          *QueryContext
	order                        []shelltask.OrderOption
	inters                       []Interceptor
	predicates                   []predicate.ShellTask
	withShell                    *ShellQuery
	withCreator                  *UserQuery
	withReportedFiles            *HostFileQuery
	withReportedProcesses        *HostProcessQuery
	withReportedCredentials      *HostCredentialQuery
	withFKs                      bool
	modifiers                    []func(*sql.Selector)
	loadTotal                    []func(context.Context, []*ShellTask) error
	withNamedReportedFiles       map[string]*HostFileQuery
	withNamedReportedProcesses   map[string]*HostProcessQuery
	withNamedReportedCredentials map[string]*HostCredentialQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ShellTaskQuery builder.
func (stq *ShellTaskQuery) Where(ps ...predicate.ShellTask) *ShellTaskQuery {
	stq.predicates = append(stq.predicates, ps...)
	return stq
}

// Limit the number of records to be returned by this query.
func (stq *ShellTaskQuery) Limit(limit int) *ShellTaskQuery {
	stq.ctx.Limit = &limit
	return stq
}

// Offset to start from.
func (stq *ShellTaskQuery) Offset(offset int) *ShellTaskQuery {
	stq.ctx.Offset = &offset
	return stq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (stq *ShellTaskQuery) Unique(unique bool) *ShellTaskQuery {
	stq.ctx.Unique = &unique
	return stq
}

// Order specifies how the records should be ordered.
func (stq *ShellTaskQuery) Order(o ...shelltask.OrderOption) *ShellTaskQuery {
	stq.order = append(stq.order, o...)
	return stq
}

// QueryShell chains the current query on the "shell" edge.
func (stq *ShellTaskQuery) QueryShell() *ShellQuery {
	query := (&ShellClient{config: stq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := stq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := stq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shelltask.Table, shelltask.FieldID, selector),
			sqlgraph.To(shell.Table, shell.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, shelltask.ShellTable, shelltask.ShellColumn),
		)
		fromU = sqlgraph.SetNeighbors(stq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCreator chains the current query on the "creator" edge.
func (stq *ShellTaskQuery) QueryCreator() *UserQuery {
	query := (&UserClient{config: stq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := stq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := stq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shelltask.Table, shelltask.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, shelltask.CreatorTable, shelltask.CreatorColumn),
		)
		fromU = sqlgraph.SetNeighbors(stq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReportedFiles chains the current query on the "reported_files" edge.
func (stq *ShellTaskQuery) QueryReportedFiles() *HostFileQuery {
	query := (&HostFileClient{config: stq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := stq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := stq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shelltask.Table, shelltask.FieldID, selector),
			sqlgraph.To(hostfile.Table, hostfile.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, shelltask.ReportedFilesTable, shelltask.ReportedFilesColumn),
		)
		fromU = sqlgraph.SetNeighbors(stq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReportedProcesses chains the current query on the "reported_processes" edge.
func (stq *ShellTaskQuery) QueryReportedProcesses() *HostProcessQuery {
	query := (&HostProcessClient{config: stq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := stq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := stq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shelltask.Table, shelltask.FieldID, selector),
			sqlgraph.To(hostprocess.Table, hostprocess.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, shelltask.ReportedProcessesTable, shelltask.ReportedProcessesColumn),
		)
		fromU = sqlgraph.SetNeighbors(stq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReportedCredentials chains the current query on the "reported_credentials" edge.
func (stq *ShellTaskQuery) QueryReportedCredentials() *HostCredentialQuery {
	query := (&HostCredentialClient{config: stq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := stq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := stq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shelltask.Table, shelltask.FieldID, selector),
			sqlgraph.To(hostcredential.Table, hostcredential.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, shelltask.ReportedCredentialsTable, shelltask.ReportedCredentialsColumn),
		)
		fromU = sqlgraph.SetNeighbors(stq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ShellTask entity from the query.
// Returns a *NotFoundError when no ShellTask was found.
func (stq *ShellTaskQuery) First(ctx context.Context) (*ShellTask, error) {
	nodes, err := stq.Limit(1).All(setContextOp(ctx, stq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{shelltask.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (stq *ShellTaskQuery) FirstX(ctx context.Context) *ShellTask {
	node, err := stq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ShellTask ID from the query.
// Returns a *NotFoundError when no ShellTask ID was found.
func (stq *ShellTaskQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = stq.Limit(1).IDs(setContextOp(ctx, stq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{shelltask.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (stq *ShellTaskQuery) FirstIDX(ctx context.Context) int {
	id, err := stq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ShellTask entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ShellTask entity is found.
// Returns a *NotFoundError when no ShellTask entities are found.
func (stq *ShellTaskQuery) Only(ctx context.Context) (*ShellTask, error) {
	nodes, err := stq.Limit(2).All(setContextOp(ctx, stq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{shelltask.Label}
	default:
		return nil, &NotSingularError{shelltask.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (stq *ShellTaskQuery) OnlyX(ctx context.Context) *ShellTask {
	node, err := stq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ShellTask ID in the query.
// Returns a *NotSingularError when more than one ShellTask ID is found.
// Returns a *NotFoundError when no entities are found.
func (stq *ShellTaskQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = stq.Limit(2).IDs(setContextOp(ctx, stq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{shelltask.Label}
	default:
		err = &NotSingularError{shelltask.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (stq *ShellTaskQuery) OnlyIDX(ctx context.Context) int {
	id, err := stq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ShellTasks.
func (stq *ShellTaskQuery) All(ctx context.Context) ([]*ShellTask, error) {
	ctx = setContextOp(ctx, stq.ctx, ent.OpQueryAll)
	if err := stq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ShellTask, *ShellTaskQuery]()
	return withInterceptors[[]*ShellTask](ctx, stq, qr, stq.inters)
}

// AllX is like All, but panics if an error occurs.
func (stq *ShellTaskQuery) AllX(ctx context.Context) []*ShellTask {
	nodes, err := stq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ShellTask IDs.
func (stq *ShellTaskQuery) IDs(ctx context.Context) (ids []int, err error) {
	if stq.ctx.Unique == nil && stq.path != nil {
		stq.Unique(true)
	}
	ctx = setContextOp(ctx, stq.ctx, ent.OpQueryIDs)
	if err = stq.Select(shelltask.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (stq *ShellTaskQuery) IDsX(ctx context.Context) []int {
	ids, err := stq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (stq *ShellTaskQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, stq.ctx, ent.OpQueryCount)
	if err := stq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, stq, querierCount[*ShellTaskQuery](), stq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (stq *ShellTaskQuery) CountX(ctx context.Context) int {
	count, err := stq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (stq *ShellTaskQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, stq.ctx, ent.OpQueryExist)
	switch _, err := stq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (stq *ShellTaskQuery) ExistX(ctx context.Context) bool {
	exist, err := stq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ShellTaskQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (stq *ShellTaskQuery) Clone() *ShellTaskQuery {
	if stq == nil {
		return nil
	}
	return &ShellTaskQuery{
		config:                  stq.config,
		ctx:                     stq.ctx.Clone(),
		order:                   append([]shelltask.OrderOption{}, stq.order...),
		inters:                  append([]Interceptor{}, stq.inters...),
		predicates:              append([]predicate.ShellTask{}, stq.predicates...),
		withShell:               stq.withShell.Clone(),
		withCreator:             stq.withCreator.Clone(),
		withReportedFiles:       stq.withReportedFiles.Clone(),
		withReportedProcesses:   stq.withReportedProcesses.Clone(),
		withReportedCredentials: stq.withReportedCredentials.Clone(),
		// clone intermediate query.
		sql:  stq.sql.Clone(),
		path: stq.path,
	}
}

// WithShell tells the query-builder to eager-load the nodes that are connected to
// the "shell" edge. The optional arguments are used to configure the query builder of the edge.
func (stq *ShellTaskQuery) WithShell(opts ...func(*ShellQuery)) *ShellTaskQuery {
	query := (&ShellClient{config: stq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	stq.withShell = query
	return stq
}

// WithCreator tells the query-builder to eager-load the nodes that are connected to
// the "creator" edge. The optional arguments are used to configure the query builder of the edge.
func (stq *ShellTaskQuery) WithCreator(opts ...func(*UserQuery)) *ShellTaskQuery {
	query := (&UserClient{config: stq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	stq.withCreator = query
	return stq
}

// WithReportedFiles tells the query-builder to eager-load the nodes that are connected to
// the "reported_files" edge. The optional arguments are used to configure the query builder of the edge.
func (stq *ShellTaskQuery) WithReportedFiles(opts ...func(*HostFileQuery)) *ShellTaskQuery {
	query := (&HostFileClient{config: stq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	stq.withReportedFiles = query
	return stq
}

// WithReportedProcesses tells the query-builder to eager-load the nodes that are connected to
// the "reported_processes" edge. The optional arguments are used to configure the query builder of the edge.
func (stq *ShellTaskQuery) WithReportedProcesses(opts ...func(*HostProcessQuery)) *ShellTaskQuery {
	query := (&HostProcessClient{config: stq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	stq.withReportedProcesses = query
	return stq
}

// WithReportedCredentials tells the query-builder to eager-load the nodes that are connected to
// the "reported_credentials" edge. The optional arguments are used to configure the query builder of the edge.
func (stq *ShellTaskQuery) WithReportedCredentials(opts ...func(*HostCredentialQuery)) *ShellTaskQuery {
	query := (&HostCredentialClient{config: stq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	stq.withReportedCredentials = query
	return stq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ShellTask.Query().
//		GroupBy(shelltask.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (stq *ShellTaskQuery) GroupBy(field string, fields ...string) *ShellTaskGroupBy {
	stq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ShellTaskGroupBy{build: stq}
	grbuild.flds = &stq.ctx.Fields
	grbuild.label = shelltask.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ShellTask.Query().
//		Select(shelltask.FieldCreatedAt).
//		Scan(ctx, &v)
func (stq *ShellTaskQuery) Select(fields ...string) *ShellTaskSelect {
	stq.ctx.Fields = append(stq.ctx.Fields, fields...)
	sbuild := &ShellTaskSelect{ShellTaskQuery: stq}
	sbuild.label = shelltask.Label
	sbuild.flds, sbuild.scan = &stq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ShellTaskSelect configured with the given aggregations.
func (stq *ShellTaskQuery) Aggregate(fns ...AggregateFunc) *ShellTaskSelect {
	return stq.Select().Aggregate(fns...)
}

func (stq *ShellTaskQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range stq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, stq); err != nil {
				return err
			}
		}
	}
	for _, f := range stq.ctx.Fields {
		if !shelltask.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if stq.path != nil {
		prev, err := stq.path(ctx)
		if err != nil {
			return err
		}
		stq.sql = prev
	}
	return nil
}

func (stq *ShellTaskQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ShellTask, error) {
	var (
		nodes       = []*ShellTask{}
		withFKs     = stq.withFKs
		_spec       = stq.querySpec()
		loadedTypes = [5]bool{
			stq.withShell != nil,
			stq.withCreator != nil,
			stq.withReportedFiles != nil,
			stq.withReportedProcesses != nil,
			stq.withReportedCredentials != nil,
		}
	)
	if stq.withShell != nil || stq.withCreator != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, shelltask.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ShellTask).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ShellTask{config: stq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(stq.modifiers) > 0 {
		_spec.Modifiers = stq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, stq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := stq.withShell; query != nil {
		if err := stq.loadShell(ctx, query, nodes, nil,
			func(n *ShellTask, e *Shell) { n.Edges.Shell = e }); err != nil {
			return nil, err
		}
	}
	if query := stq.withCreator; query != nil {
		if err := stq.loadCreator(ctx, query, nodes, nil,
			func(n *ShellTask, e *User) { n.Edges.Creator = e }); err != nil {
			return nil, err
		}
	}
	if query := stq.withReportedFiles; query != nil {
		if err := stq.loadReportedFiles(ctx, query, nodes,
			func(n *ShellTask) { n.Edges.ReportedFiles = []*HostFile{} },
			func(n *ShellTask, e *HostFile) { n.Edges.ReportedFiles = append(n.Edges.ReportedFiles, e) }); err != nil {
			return nil, err
		}
	}
	if query := stq.withReportedProcesses; query != nil {
		if err := stq.loadReportedProcesses(ctx, query, nodes,
			func(n *ShellTask) { n.Edges.ReportedProcesses = []*HostProcess{} },
			func(n *ShellTask, e *HostProcess) { n.Edges.ReportedProcesses = append(n.Edges.ReportedProcesses, e) }); err != nil {
			return nil, err
		}
	}
	if query := stq.withReportedCredentials; query != nil {
		if err := stq.loadReportedCredentials(ctx, query, nodes,
			func(n *ShellTask) { n.Edges.ReportedCredentials = []*HostCredential{} },
			func(n *ShellTask, e *HostCredential) {
				n.Edges.ReportedCredentials = append(n.Edges.ReportedCredentials, e)
			}); err != nil {
			return nil, err
		}
	}
	for name, query := range stq.withNamedReportedFiles {
		if err := stq.loadReportedFiles(ctx, query, nodes,
			func(n *ShellTask) { n.appendNamedReportedFiles(name) },
			func(n *ShellTask, e *HostFile) { n.appendNamedReportedFiles(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range stq.withNamedReportedProcesses {
		if err := stq.loadReportedProcesses(ctx, query, nodes,
			func(n *ShellTask) { n.appendNamedReportedProcesses(name) },
			func(n *ShellTask, e *HostProcess) { n.appendNamedReportedProcesses(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range stq.withNamedReportedCredentials {
		if err := stq.loadReportedCredentials(ctx, query, nodes,
			func(n *ShellTask) { n.appendNamedReportedCredentials(name) },
			func(n *ShellTask, e *HostCredential) { n.appendNamedReportedCredentials(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range stq.loadTotal {
		if err := stq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (stq *ShellTaskQuery) loadShell(ctx context.Context, query *ShellQuery, nodes []*ShellTask, init func(*ShellTask), assign func(*ShellTask, *Shell)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ShellTask)
	for i := range nodes {
		if nodes[i].shell_shell_tasks == nil {
			continue
		}
		fk := *nodes[i].shell_shell_tasks
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(shell.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "shell_shell_tasks" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (stq *ShellTaskQuery) loadCreator(ctx context.Context, query *UserQuery, nodes []*ShellTask, init func(*ShellTask), assign func(*ShellTask, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ShellTask)
	for i := range nodes {
		if nodes[i].shell_task_creator == nil {
			continue
		}
		fk := *nodes[i].shell_task_creator
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "shell_task_creator" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (stq *ShellTaskQuery) loadReportedFiles(ctx context.Context, query *HostFileQuery, nodes []*ShellTask, init func(*ShellTask), assign func(*ShellTask, *HostFile)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*ShellTask)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.HostFile(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(shelltask.ReportedFilesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.shell_task_reported_files
		if fk == nil {
			return fmt.Errorf(`foreign-key "shell_task_reported_files" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "shell_task_reported_files" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (stq *ShellTaskQuery) loadReportedProcesses(ctx context.Context, query *HostProcessQuery, nodes []*ShellTask, init func(*ShellTask), assign func(*ShellTask, *HostProcess)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*ShellTask)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.HostProcess(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(shelltask.ReportedProcessesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.shell_task_reported_processes
		if fk == nil {
			return fmt.Errorf(`foreign-key "shell_task_reported_processes" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "shell_task_reported_processes" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (stq *ShellTaskQuery) loadReportedCredentials(ctx context.Context, query *HostCredentialQuery, nodes []*ShellTask, init func(*ShellTask), assign func(*ShellTask, *HostCredential)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*ShellTask)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.HostCredential(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(shelltask.ReportedCredentialsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.shell_task_reported_credentials
		if fk == nil {
			return fmt.Errorf(`foreign-key "shell_task_reported_credentials" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "shell_task_reported_credentials" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (stq *ShellTaskQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := stq.querySpec()
	if len(stq.modifiers) > 0 {
		_spec.Modifiers = stq.modifiers
	}
	_spec.Node.Columns = stq.ctx.Fields
	if len(stq.ctx.Fields) > 0 {
		_spec.Unique = stq.ctx.Unique != nil && *stq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, stq.driver, _spec)
}

func (stq *ShellTaskQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(shelltask.Table, shelltask.Columns, sqlgraph.NewFieldSpec(shelltask.FieldID, field.TypeInt))
	_spec.From = stq.sql
	if unique := stq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if stq.path != nil {
		_spec.Unique = true
	}
	if fields := stq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, shelltask.FieldID)
		for i := range fields {
			if fields[i] != shelltask.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := stq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := stq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := stq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := stq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (stq *ShellTaskQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(stq.driver.Dialect())
	t1 := builder.Table(shelltask.Table)
	columns := stq.ctx.Fields
	if len(columns) == 0 {
		columns = shelltask.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if stq.sql != nil {
		selector = stq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if stq.ctx.Unique != nil && *stq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range stq.predicates {
		p(selector)
	}
	for _, p := range stq.order {
		p(selector)
	}
	if offset := stq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := stq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedReportedFiles tells the query-builder to eager-load the nodes that are connected to the "reported_files"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (stq *ShellTaskQuery) WithNamedReportedFiles(name string, opts ...func(*HostFileQuery)) *ShellTaskQuery {
	query := (&HostFileClient{config: stq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if stq.withNamedReportedFiles == nil {
		stq.withNamedReportedFiles = make(map[string]*HostFileQuery)
	}
	stq.withNamedReportedFiles[name] = query
	return stq
}

// WithNamedReportedProcesses tells the query-builder to eager-load the nodes that are connected to the "reported_processes"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (stq *ShellTaskQuery) WithNamedReportedProcesses(name string, opts ...func(*HostProcessQuery)) *ShellTaskQuery {
	query := (&HostProcessClient{config: stq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if stq.withNamedReportedProcesses == nil {
		stq.withNamedReportedProcesses = make(map[string]*HostProcessQuery)
	}
	stq.withNamedReportedProcesses[name] = query
	return stq
}

// WithNamedReportedCredentials tells the query-builder to eager-load the nodes that are connected to the "reported_credentials"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (stq *ShellTaskQuery) WithNamedReportedCredentials(name string, opts ...func(*HostCredentialQuery)) *ShellTaskQuery {
	query := (&HostCredentialClient{config: stq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if stq.withNamedReportedCredentials == nil {
		stq.withNamedReportedCredentials = make(map[string]*HostCredentialQuery)
	}
	stq.withNamedReportedCredentials[name] = query
	return stq
}

// ShellTaskGroupBy is the group-by builder for ShellTask entities.
type ShellTaskGroupBy struct {
	selector
	build *ShellTaskQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (stgb *ShellTaskGroupBy) Aggregate(fns ...AggregateFunc) *ShellTaskGroupBy {
	stgb.fns = append(stgb.fns, fns...)
	return stgb
}

// Scan applies the selector query and scans the result into the given value.
func (stgb *ShellTaskGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, stgb.build.ctx, ent.OpQueryGroupBy)
	if err := stgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShellTaskQuery, *ShellTaskGroupBy](ctx, stgb.build, stgb, stgb.build.inters, v)
}

func (stgb *ShellTaskGroupBy) sqlScan(ctx context.Context, root *ShellTaskQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(stgb.fns))
	for _, fn := range stgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*stgb.flds)+len(stgb.fns))
		for _, f := range *stgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*stgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := stgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ShellTaskSelect is the builder for selecting fields of ShellTask entities.
type ShellTaskSelect struct {
	*ShellTaskQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sts *ShellTaskSelect) Aggregate(fns ...AggregateFunc) *ShellTaskSelect {
	sts.fns = append(sts.fns, fns...)
	return sts
}

// Scan applies the selector query and scans the result into the given value.
func (sts *ShellTaskSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sts.ctx, ent.OpQuerySelect)
	if err := sts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShellTaskQuery, *ShellTaskSelect](ctx, sts.ShellTaskQuery, sts, sts.inters, v)
}

func (sts *ShellTaskSelect) sqlScan(ctx context.Context, root *ShellTaskQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sts.fns))
	for _, fn := range sts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
