enum StructuredData {
    FILE
    PROCESS
}

type TaskDiff {
    ids: [ID!]!
    output: String
    error: String
    structuredData: [StructuredData!]!
}

extend type Quest {
    diffs: [TaskDiff!]! @goField(forceResolver: true)
}
