def cat(path):
    if file.is_file(path):
        res = file.read(path)
        print(res)
    else:
        eprint(f"Error: Invalid Path '{path}'")
    return

time.sleep(5)
cat(input_params['path'])
