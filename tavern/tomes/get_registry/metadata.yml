name: Get Registry
description: List the subkeys and their values at a provided hive and path.
author: hulto
support_model: FIRST_PARTY
tactic: RECON
paramdefs:
- name: path
  type: string
  label: Registry key path
  placeholder: "HKEY_LOCAL_MACHINE\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion" # Single backslash can be used too but you may encounter issues if you specify "\x64" as that's hex.
